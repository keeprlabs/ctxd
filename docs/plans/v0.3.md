# ctxd v0.3 — Federation, Backends, and Real Adapters

You are leading the implementation of ctxd v0.3 — the federation + backends + real adapters release for an open-source Rust context substrate. The repo is a Cargo workspace with 10 crates; v0.2 is shipped and on `main`. You are working in a git worktree automatically created from `/Users/manikandan/Desktop/retry/ctxd`. Base branch: `main`.

Treat this as low-level systems work. You are writing code that will run as a daemon on other people's machines, replicate data over the network, enforce capability-based security, and must survive restarts and concurrent writes. Think like you are writing kernel code: invariants are sacred, tests prove correctness, perf is measured not guessed, comments explain WHY not WHAT.

## WHAT TO BUILD

Implement the ctxd v0.3 plan verbatim, in the phased order below. Each phase has a milestone exit criterion — do NOT advance until the exit is met. After each phase: run the full test suite, run clippy, run fmt check, run benchmarks where relevant, update docs, create a tagged commit, then proceed.

### PHASE 1 — FOUNDATION (4 days of work)
- 1A. Introduce a `Store` trait in a new crate `ctxd-store-core` with methods: append, read, read_at, subjects, search, kv_get, kv_get_at, entities_query, relationships_for, peer_add, peer_list, peer_remove, peer_cursor_set, peer_cursor_get, revoke_token, is_token_revoked, vector_upsert, vector_search. Rename existing `ctxd-store` contents into `ctxd-store-sqlite` crate; keep `ctxd-store` as a re-export shim for back-compat. Write a shared conformance test suite in `ctxd-store-core/src/testsuite.rs` that every backend runs.
- 1B. Add `parents: Vec<Uuid>` and `attestation: Option<Vec<u8>>` to `Event` in `ctxd-core/src/event.rs`. Include both unconditionally in canonical form for hash and signature. Update all serde, hashing, signing code. Parents sorted by id string; attestation hex-encoded if present.
- 1C. Implement `ctxd migrate --to 0.3 [--dry-run]` that re-computes predecessor hashes and re-signs events with the stored signing key, in batches of 1000 per transaction. Add `metadata.ctxd_version` row for idempotency. Must handle an unmigrated v0.2 db correctly.
- 1D. Wire `ctx_entities`, `ctx_related`, `ctx_timeline` MCP tools in `ctxd-mcp/src/server.rs` against the existing store APIs. Update `docs/mcp.md` to remove the (v0.2) tag.
- 1E. Schema additions to SQLite: add `parents TEXT`, `attestation BLOB` columns to events; new tables `event_parents`, `peers`, `peer_cursors`, `token_budgets`, `pending_approvals`, `vector_embeddings`. All migrations additive.

EXIT M1: existing tests pass against the trait object; migrate command works round-trip on a pre-v0.3 db; graph/temporal tools respond in mcp-inspector (you can test programmatically).

### PHASE 2 — FEDERATION + THIRD-PARTY CAVEATS (9 days of work)
- 2A. Add `PeerHello`, `PeerReplicate`, `PeerCursorRequest`, `PeerWelcome`, `PeerAck`, `PeerCursor`, `PeerFetchEvents` variants to the wire protocol Request/Response enums in `ctxd-cli/src/protocol.rs`.
- 2B. Automatic capability exchange on `ctxd peer add --url <url> [--subjects <globs>] [--auto-accept]`. Local mints cap for remote, sends in PeerHello; remote mints reciprocal cap, returns in PeerWelcome. Config knob `federation.auto_accept = true | false | allowlist<pubkey>`.
- 2C. Upgrade `ctxd-cap` to use biscuit-auth third-party blocks. Add `attenuate_with_block`, `verify_multi(token, &[known_authorities], subject, op)`. Spike a three-authority chain test first. Peer store holds trusted authority pubkeys.
- 2D. Replication stream in new `ctxd-cli/src/federation.rs` — `PeerManager` owns per-peer connections, each with outbound filter loop (subscribe to local broadcast, filter by peer's granted subject glob intersected with its cap scope, skip events originating from this peer) and inbound handler (verify sig against peer pubkey, accept concurrent-branch events, idempotent append, ACK).
- 2E. Cursor resume: on reconnect, send PeerCursorRequest, remote returns last-seen event id/time, local streams events with time > cursor matching subject patterns. Update cursor on each ACK. Parent-backfill via PeerFetchEvents when an incoming event references unknown parents.
- 2F. Conflict resolution: log keeps both branches. KV view uses LWW on (time, event_id) with UUIDv7 lexical tiebreak. Deterministic across peers. `read` returns both concurrent branches ordered.
- 2G. CLI: `ctxd peer add`, `peer list`, `peer status`, `peer remove`, `peer grant`.
- 2H. Docs: `docs/federation.md` with two-node tutorial, ADRs 006–009.

EXIT M2: two in-process nodes pair automatically, bidirectionally replicate, survive restart with correct resume, handle concurrent writes deterministically. Third-party attenuation chain verified end-to-end.

### PHASE 3 — STATEFUL CAVEATS + MULTI-TRANSPORT MCP (6 days of work)
- 3A. `CaveatState` trait in `ctxd-cap/src/state.rs` with budget_increment, rate_check, approval_request, approval_wait, approval_decide. SQLite-backed impl. Existing RateLimiter becomes in-memory fast path.
- 3B. `BudgetLimit(currency, amount_micro_units)` biscuit caveat. Per-operation costs defined at a single source of truth. Exceed = `CapError::BudgetExceeded`. Document that x402 HTTP 402 gateway integration is v0.4.
- 3C. `HumanApprovalRequired(op)` caveat. Blocks up to configurable timeout. `ctxd approve <id> --decision allow|deny` and `POST /v1/approvals/:id/decide`. Broadcast channel for notifier adapters (no notifier shipped).
- 3D. Multi-transport MCP: run stdio + SSE + streamable-HTTP concurrently from the same `CtxdMcpServer`. CLI flags `--mcp-stdio`, `--mcp-sse <addr>`, `--mcp-http <addr>`. SSE/HTTP accept token via `Authorization: Bearer` and tool-arg fallback.
- 3E. Docs: update `docs/mcp.md` with transports + auth, ADRs 010–011.

EXIT M3: mcp-inspector or equivalent connects over all three transports, same tools. Budget + approval caveats enforce end-to-end in integration tests.

### PHASE 4 — EMBEDDINGS + REAL ADAPTERS + TEE FIELD (8 days of work)
- 4A. New crate `ctxd-embed` with `Embedder` trait. Impls: `NullEmbedder` (default), `OpenAiEmbedder`, `OllamaEmbedder`. CLI: `--embedder`, `--embedder-model`, `--embedder-url`.
- 4B. Vector persistence: migrate from `instant-distance` to `hnsw_rs`. Persist index to `<db>.hnsw`. Raw vectors in `vector_embeddings` table. Rebuild on startup if missing/corrupt. `ctx_search` gets `search_mode: fts|vector|hybrid`, default hybrid when embedder configured.
- 4C. Real Gmail adapter: OAuth2 device flow, encrypted token at rest (AES-256-GCM with HKDF-derived key), Gmail History API incremental sync, rate-limit honoring. Events under `/work/email/gmail/{label}/{message_id}`. Binary: `ctxd-adapter-gmail`. NOTE: OAuth testing requires real Google Cloud credentials — write the flow, exercise with mocks, document manual test procedure.
- 4D. Real GitHub adapter: PAT auth, polling issues/PRs/comments/notifications with `since` + ETag, honoring `X-RateLimit-*`. Subjects `/work/github/{owner}/{repo}/{type}/{id}`.
- 4E. TEE attestation passthrough: adapters can set it; federation replicates unchanged; optional `attestation_verifier: Option<Box<dyn Fn(&[u8]) -> bool>>` hook in CapEngine::verify. Document in ADR 012 that actual TEE SDK integration is v0.4.
- 4F. Docs: `docs/embeddings.md`, `docs/adapters/gmail.md`, `docs/adapters/github.md`; update README quickstart.

EXIT M4: a test exercises Gmail adapter against a mock Gmail server ingesting into ctxd with embeddings; vector index survives restart; ctx_search hybrid returns sensible ranking.

### PHASE 5 — ADDITIONAL BACKENDS (6 days of work)
- 5A. `ctxd-store-postgres` crate. Postgres-idiomatic schema (BYTEA, JSONB, TIMESTAMPTZ, GIN). FTS via tsvector + generated column + GIN. Migrations under `ctxd-store-postgres/migrations/`. CI: Postgres service container in GitHub Actions.
- 5B. `ctxd-store-duckobj` crate. Event log as append-only Parquet on object store (start local filesystem via the `object_store` crate, S3/R2 later). Partitioned by subject_root/yyyy-mm. Size-based rotation at 64 MB. Materialized views live in a local SQLite sidecar. Writes batched 1s or 1000 events. Read path merges sealed partitions with in-memory batch.
- 5C. Wire `ctxd serve --storage sqlite|postgres|duckdb-object` selector. All three pass the shared conformance suite.

EXIT M5: `cargo test --workspace` passes for all three backends; CI matrix green.

## QUALITY BAR

- No `unwrap()` or `expect()` in library crates. Binary + tests are fine.
- No `TODO`/`FIXME` without an issue link or explicit tracking in docs/plans/v0.3.md.
- Every public item in a library has rustdoc; every trait has doctests where feasible.
- `cargo clippy -- -D warnings` passes at every commit.
- `cargo fmt --check` passes at every commit.
- Meaningful tests, not placeholders. When in doubt, add a property-based test with `proptest`.
- Use `#[tracing::instrument]` on hot-path async functions.
- Feature flags for heavy optional deps (postgres, duckdb, openai, ollama) so a default build stays lean.

## TESTING DISCIPLINE

- Every trait method has at least one conformance test in the shared suite.
- Federation: two in-process daemons via `examples/federation-quickstart` and an integration test spinning two ProtocolServers on ephemeral ports. Assert event round-trip identity (same id, hash, sig, parents on both sides), cursor resume correctness, conflict resolution determinism.
- Biscuit third-party blocks: explicit three-authority chain test before integrating into peer code.
- Backends: conformance matrix via `CI_BACKEND={sqlite,postgres,duckdb}` env.
- Security-relevant tests: wrong key, tampered event, widened capability, expired cap, revoked token — all must reject.

## BENCHMARKING DISCIPLINE

- Re-run the existing benchmark suite (`crates/ctxd-store/benches/store_bench.rs`) after Phase 1 (baseline with trait-object overhead) and Phase 5 (cross-backend).
- Add new benches for: federation replication throughput, third-party block verification latency, vector search latency vs FTS.
- Update `docs/benchmark-results.md` at the end of each phase.

## HARD RULES

- Don't stop until all five phases are complete and green, OR you hit a genuine blocker that requires a human decision.
- If you hit context limits, commit what you have, write a resumption note in `docs/plans/v0.3-progress.md`, and end with a summary of where to pick up.
- Never introduce a dependency that isn't necessary. Prefer stdlib, then already-in-tree crates, then well-known crates.
- Prefer small, deliberate commits over one giant commit per phase.
- If a decision is ambiguous at implementation time, write an ADR under `docs/decisions/`, make a call consistent with the spirit of the plan, and keep moving.
