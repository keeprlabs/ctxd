// Step 7 replaces this with the full router + view renderers.
// Stub so the rust-embed wiring has something to ship in step 5.
document.querySelector('#app').innerHTML =
  '<p style="color:#999">ctxd dashboard — frontend lands in step 7.</p>';
